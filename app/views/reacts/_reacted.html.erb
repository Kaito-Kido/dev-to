<%= link_to post_react_path(post), remote: true, method: :delete, class: "react-button", id: "like-#{post.id}", data: { "post-id": post.id } do%>
  <span class="liked">
    <img src="/assets/reacted.svg">
  </span>
  <p class="liked"><%= post.reacts_count%> Liked</p>
<%end%>
